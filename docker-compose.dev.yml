version: '3.4'
services:
  dev-api:
    build:
      context: .
      target: development
    volumes:
      - ./:/usr/src/app
      - /usr/src/app/node_modules
    ports:
      - 4040:4040
    command: yarn dev